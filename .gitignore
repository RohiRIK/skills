# macOS
.DS_Store

# Local working dirs — not committed
specs/
plans/
tmp/

# Editor / runtime cruft
*.log
.env
.env.*
!.env.schema

# Node / bun deps and generated report output
node_modules/
*.xlsx

# Personal one-off report scripts (hardcoded private paths — not library material)
DataReportBuilder/Tools/build-*-report.ts

# build staging (donor downloads, zips, scratch)
doc/
docs/files.zip
skills-update.zip
.agent-state.md
